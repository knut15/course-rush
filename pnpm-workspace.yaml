# 신청 서버와 부하 생성기를 다른 패키지로 나눈다.
# 나누는 이유는 취향이 아니라 측정이다 — 같은 프로세스에서 돌리면
# p99 가 신청 처리 시간이 아니라 부하 생성 비용까지 함께 재게 된다(GOAL.md §6).
packages:
  - "server"
  - "loadgen"
  - "web"

allowBuilds:
  # esbuild 는 postinstall 로 플랫폼 바이너리를 받는다. 막으면 tsx 가 뜨지 않는다.
  esbuild: true
