# GOAL — course-rush : 수강신청 동시성 데모

> 이 문서는 course-rush 제작의 단일 기준이다. 아래 "확정 사항" 은 인터뷰로 굳힌 것이므로 되묻지 않는다.
> 작성일 2026-09-22.

---

## 0. 한 줄 목표

`~/Workspace/course-rush` 에 **수강신청 동시성 문제를 다섯 가지 방식으로 풀어 나란히 비교하는 데모**를 만든다.
버튼을 누르면 같은 부하가 각 방식에 던져지고, **정원 초과 건수와 응답시간 백분위가 한 표에 쌓인다.**

이 데모가 증명할 것은 "1만 명을 처리했다" 가 아니라 **"몰릴 때 무엇이 깨지는지 알고, 왜 이 방법을 골랐는지 설명할 수 있다"** 이다.

---

## 1. 확정 사항 (인터뷰 3라운드, 변경 금지)

| 항목 | 결정 |
|---|---|
| 데모 범위 | **동시성 5종 + 대기열** |
| 부하 생성 | **서버 내부 생성기(인터랙티브) + k6(신뢰 수치)** 병행 |
| 스택 | **Next + Express 분리.** 부하 생성기와 신청 서버가 다른 프로세스여야 측정이 오염되지 않는다 |
| 산출물 | **데모 페이지 + 발표 슬라이드 + README** |
| "1만" 의 정의 | **총 1만 요청 / 동시 500–2000.** 동시 커넥션 1만이 아니다 |
| k6 실행 | **Docker 이미지 `grafana/k6`.** 로컬에 바이너리를 설치하지 않는다 |
| 실시간 갱신 | **SSE** |
| 배포 | **로컬에서만** |
| 도메인 규칙 | **정원 + 중복신청 방지** 두 가지 |
| 결과 화면 | **비교표 + 응답시간 분포 차트** |
| 진행 방식 | **단계별로 끊어 확인.** 각 단계 끝에 수치를 보고하고 다음으로 넘어간다 |

---

## 2. 기본값 (바꾸려면 이 표만 고친다)

| 항목 | 값 | 근거 |
|---|---|---|
| 산출물 언어 | 본문·주석·문서 전부 **한글** | `~/Workspace/CLAUDE.md` 규칙 1 |
| 패키지 매니저 | **pnpm 고정** | CLAUDE.md 규칙 6 |
| 레포 구조 | pnpm 워크스페이스 (`server` / `web`) | board-api 와 같은 형태. 익숙한 구조가 발표 준비 시간을 아낀다 |
| Postgres 포트 | **5433** | board-api 컨테이너가 5432 를 쓰는 중 |
| Redis 포트 | 6379 | 충돌 없음 |
| 컨테이너 실행 | `docker-compose` (하이픈) | 이 머신에서 `docker compose` 서브커맨드가 동작하지 않는다 |
| 측정 환경 | **10코어 / 16GB / macOS, 전부 로컬** | 모든 수치 옆에 이 조건을 함께 적는다 |
| 기본 시나리오 | 과목 정원 **50**, 총 요청 **10,000**, 동시 **1,000** | 화면에서 조절 가능 |

---

## 3. 산출물 3개

| # | 산출물 | 위치 | 완료 조건 |
|---|---|---|---|
| 1 | 데모 페이지 | `web/` | 브라우저에서 방식을 골라 부하를 던지고 비교표·차트가 갱신된다 |
| 2 | README | `README.md` | 다섯 방식의 SQL 과 선택 근거, 재현 명령, 측정 수치가 있다 |
| 3 | 발표 슬라이드 | `slides/` | §9 의 발표 구성대로. **수치는 실제 측정값만 싣는다** |

---

## 4. 핵심 — 다섯 가지 구현

전부 같은 인터페이스를 구현하고 런타임에 갈아 끼운다. 쿼리 파라미터 `?mode=M1` 로 고른다.

### M1. 순진한 구현 — 깨지는 것을 보여주는 용도

```sql
SELECT count(*) FROM enrollments WHERE course_id = $1;   -- 판정
-- 애플리케이션에서 if (count < capacity)
INSERT INTO enrollments (course_id, student_id) VALUES ($1, $2);
```

읽기와 쓰기 사이에 다른 요청이 끼어든다(TOCTOU). **정원이 깨진다.**
`UNIQUE(course_id, student_id)` 는 중복신청만 막고 정원은 못 막는다 — 이 대비가 발표 소재다.

### M2. 비관적 락

```sql
BEGIN;
SELECT capacity FROM courses WHERE id = $1 FOR UPDATE;   -- 행을 잠근다
SELECT count(*) FROM enrollments WHERE course_id = $1;
INSERT INTO enrollments (course_id, student_id) VALUES ($1, $2);
COMMIT;
```

정확하다. 대신 같은 행에 전부 줄을 서고, 그동안 DB 커넥션을 붙잡는다. **p99 가 폭발하는 것을 보여주는 것이 이 방식의 존재 이유다.**

### M3. 낙관적 락

```sql
SELECT enrolled_count, version FROM courses WHERE id = $1;
-- if enrolled_count < capacity
UPDATE courses SET enrolled_count = enrolled_count + 1, version = version + 1
  WHERE id = $1 AND version = $2;
-- 영향 행 0 이면 재시도 (상한 N회)
```

**재시도율을 반드시 측정해 표에 싣는다.** "낙관적 락이 항상 낫다" 를 수치로 뒤집는 자리다.

### M4. DB 원자적 갱신

```sql
BEGIN;
UPDATE courses SET enrolled_count = enrolled_count + 1
  WHERE id = $1 AND enrolled_count < capacity
RETURNING enrolled_count;
-- 영향 행 0 이면 마감
INSERT INTO enrollments (course_id, student_id) VALUES ($1, $2);
COMMIT;
```

읽기·판정·쓰기가 DB 안에서 한 번에 끝난다. **대부분의 실무 답이 여기다.**

주의 — 중복신청 규칙 때문에 트랜잭션이 필요하다. `INSERT` 가 UNIQUE 위반으로 실패하면
이미 올린 `enrolled_count` 를 되돌려야 한다. **정원만 있을 때는 한 문장이던 것이 규칙 하나로 트랜잭션이 되는 것**,
이 지점이 발표에서 설명할 값어치가 있다.

### M5. Redis 선점

정원 차감과 중복 검사를 **하나의 Lua 스크립트**로 묶어야 원자적이다. 두 명령으로 나누면 그 사이가 다시 깨진다.

```lua
-- KEYS[1] = course:{id}:remaining, KEYS[2] = course:{id}:students
-- ARGV[1] = studentId
if redis.call('SISMEMBER', KEYS[2], ARGV[1]) == 1 then return -2 end   -- 중복
local remaining = redis.call('DECR', KEYS[1])
if remaining < 0 then
  redis.call('INCR', KEYS[1])                                          -- 되돌린다
  return -1                                                            -- 마감
end
redis.call('SADD', KEYS[2], ARGV[1])
return remaining
```

성공하면 DB 에 반영한다. **이 방식의 약점을 README 와 발표에 먼저 적는다** —
Redis 는 성공했는데 DB 반영 전에 프로세스가 죽으면 좌석이 증발한다. 메우는 방법(멱등 키, 재조정 배치)까지 적는다.

### 5종 공통 — 표에 싣는 것

| 열 | 뜻 |
|---|---|
| 등록됨 / 초과 | `enrollments` 실제 행 수와 `capacity` 의 차. **0이 아니면 나머지 수치는 의미 없다** |
| 중복 | 같은 (course, student) 가 2건 이상인 수 |
| p50 / p95 / p99 | **평균은 쓰지 않는다** |
| RPS | |
| 마감 응답 수 | **에러가 아니다.** 아래 참고 |
| 에러 수 | 타임아웃·커넥션 고갈·5xx 만 |
| 재시도 | M3 전용 |

**"정원 마감" 을 에러율에 섞지 않는다.** 섞으면 정원을 안 지키는 M1 이 에러율이 낮아 유리해 보인다.
이 구분 자체가 발표에서 설명할 지점이다.

---

## 5. 대기열

```
접속 → Redis Sorted Set 에 토큰 등록(score = 타임스탬프) → 순번 반환
     → SSE 로 "앞에 N명 · 예상 M분" 갱신
     → 워커가 초당 K명씩 앞에서 꺼내 입장 토큰 발급(TTL 5분)
     → 입장 토큰이 있는 요청만 신청 API 통과
```

**요점: 대기열은 신청 서버가 아니라 DB 를 지킨다.** 화면에서 유입 속도 K 를 슬라이더로 바꾸면
K 를 낮출 때 대기 시간이 늘고 p99 가 안정되는 것이, K 를 올릴 때 그 반대가 되는 것이 그대로 보여야 한다.
**이 슬라이더가 대기열 시연의 전부다.**

발표에서 다룰 트레이드오프: 이탈자 처리(TTL), 새로고침 시 순번 유지, Redis 가 죽었을 때, 순번을 알려주는 것과 아닌 것의 차이.

---

## 6. 부하 생성과 측정

### 두 경로

| 경로 | 무엇 | 왜 |
|---|---|---|
| 내부 생성기 | Next 쪽에서 별도 프로세스로 발사. 동시성 세마포어로 N개 유지, 총 M개 | 버튼 하나로 도는 인터랙티브 데모 |
| k6 (Docker) | `docker run --rm -i grafana/k6 run - < script.js` | 발표에 실을 수치. 스크립트와 리포트를 레포에 커밋 |

**신청 서버(Express)와 부하 생성기는 반드시 다른 프로세스여야 한다.** 같은 프로세스면 p99 가
신청 처리 시간이 아니라 부하 생성 비용까지 함께 재게 된다.

### 측정에서 지킬 것

- keep-alive agent 를 재사용하고 `maxSockets` 를 동시성에 맞춘다. 안 그러면 커넥션 수립 비용을 재게 된다
- 매 실행 전 DB·Redis 상태를 초기화한다. 초기화 여부가 결과를 바꾸면 그 측정은 못 쓴다
- 각 방식마다 워밍업 1회를 버리고 측정한다 (JIT·커넥션 풀 예열)
- **DB 커넥션 풀 크기를 화면에서 바꿀 수 있게 한다.** M2 의 p99 가 락 대기 때문인지 풀 고갈 때문인지
  구분하려면 이 변수가 필요하다

---

## 7. 데이터 모델

```
courses      id, code, title, capacity, enrolled_count, version
students     id, name
enrollments  id, course_id, student_id, created_at
             UNIQUE (course_id, student_id)
             INDEX (course_id)
```

`enrolled_count` 와 `version` 은 M3·M4 전용이다. M1·M2 는 `count(*)` 로 센다 —
**같은 테이블에 두 방식이 공존하는 것 자체가 비교의 조건이다.**

시드: 학생 10,000명, 과목 1개(정원 50). 요청마다 다른 학생 id 를 써야 중복신청 방지 규칙이 실제로 검사된다.
중복 검사를 일부러 태우려면 학생 id 를 일부 겹치게 뽑는 옵션을 둔다.

---

## 8. 화면

| 화면 | 내용 |
|---|---|
| `/` 비교 데모 | 방식 선택 · 총 요청 수 · 동시성 · 풀 크기 입력 → [발사] → 비교표 + p50/p95/p99 분포 차트 |
| `/queue` 대기열 | 유입 속도 슬라이더 · 대기 순번 화면(SSE) · 입장 현황 |

차트는 응답시간 분포(백분위 막대). **평균이 숨기는 것을 눈으로 보이게 하는 것이 차트의 목적**이므로
꺾은선 추이가 아니라 분포를 그린다.

---

## 9. 단계 계획 — 각 단계 끝에 수치를 보고하고 멈춘다

| 단계 | 내용 | 검증 |
|---|---|---|
| 1 | 워크스페이스 골격 · Postgres · 스키마 · 시드 · **M1** · 내부 부하 생성기 | **정원 50에 초과 등록이 실제로 나는 수치.** 안 나면 부하가 부족한 것이므로 동시성을 올린다 |
| 2 | **M2 · M3 · M4** · 측정 지표 수집 | 세 방식 모두 초과 0 · 중복 0. M2 의 p99 가 M4 보다 유의미하게 큼. M3 재시도율 |
| 3 | Redis · **M5** (Lua) | 초과 0 · 중복 0 · p99 가 M4 보다 작음 |
| 4 | k6 스크립트 · Docker 실행 · 리포트 커밋 | 내부 생성기 수치와 k6 수치를 대조한 표 |
| 5 | 결과 화면 (비교표 + 분포 차트) | 브라우저에서 실제 표시 확인 |
| 6 | 대기열 · SSE · 유입 속도 슬라이더 | K 를 바꿨을 때 p99 가 실제로 변하는 수치 |
| 7 | README · 슬라이드 | 문서의 모든 수치가 4~6단계 측정값과 일치 |

### 발표 구성 (7단계 산출물)

| 순서 | 내용 | 시간 |
|---|---|---|
| 1 | 문제 정의 — 정합성과 처리량은 다른 문제다 | 2분 |
| 2 | **라이브: 순진한 구현이 깨진다** | 2분 |
| 3 | 왜 깨지는가 — 격리 수준으로는 왜 부족한가 | 3분 |
| 4 | 네 가지 수정안과 수치 | 5분 |
| 5 | 왜 M4 를 골랐나 — 트레이드오프 | 3분 |
| 6 | **라이브: 대기열 유입 속도** | 3분 |
| 7 | **이 구조의 한계와 다음 단계** | 2분 |

7번을 빼지 않는다. 먼저 말하면 그 질문이 사라지고 신뢰가 남는다.

준비할 예상 질문: 격리 수준을 SERIALIZABLE 로 올리면 되지 않나 / 낙관락과 비관락 중 뭐가 낫나 /
Redis 없이 DB 만으로 어디까지 / 좌석이 증발하면 어떻게 복구하나.

---

## 10. 하지 않을 것

- 로그인·인증. 학생 id 를 요청 본문에 직접 싣는다
- 배포. 로컬에서만 돈다
- 결제·시간표 충돌·선수과목·학점 상한
- 메시지 큐(Kafka 등)·마이크로서비스 분리
- **동시 커넥션 1만 달성.** 이 머신에서 나오지 않는다

---

## 11. 정직성 규칙 — 어기면 데모 전체의 신뢰가 무너진다

1. **측정하지 않은 수치를 문서·슬라이드에 쓰지 않는다.** 확인 못 한 값은 "확인 불가" 로 남긴다
2. 모든 수치 옆에 **측정 환경(10코어/16GB/로컬)과 동시성 수준**을 함께 적는다
3. **"동시접속 1만" 이라고 쓰지 않는다.** "총 1만 요청 / 동시 1,000" 으로 쓴다
4. M5 의 약점(좌석 증발)을 README 와 슬라이드에 **먼저** 적는다
5. 내부 생성기 수치와 k6 수치가 다르면 **다르다고 적고 왜 다른지 쓴다.** 유리한 쪽만 싣지 않는다
